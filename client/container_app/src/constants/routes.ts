enum Routes {
  LOGIN = "/login",
  DASHBOARD = "/",
  DOCUMENTS = "/documents",
  DOCUMENT_DETAILS = "/documents/:id",
  KANBAN = "/kanban",
  KANBAN_DONE = "/kanban/done",
  KANBAN_IN_PROGRESS = "/kanban/in-progress",
  KANBAN_DOING = "/kanban/doing",
  KANBAN_ISSUE_DETAILS = "/kanban/:id",
}

export default Routes;
